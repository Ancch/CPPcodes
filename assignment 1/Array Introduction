#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

//You will be given an array of  integers and you have to print the integers in the reverse order.
int main() 
{
    int n;
    cin>>n;
    int a[n];
    int i;
    //array input
    for(i=0;i<n;i++)
        cin>>a[i];
    //array reverse
    for(i=n-1;i>=0;i--)
        cout<<a[i]<<" ";
    
    
    return 0;
}

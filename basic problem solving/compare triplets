#include<bits/stdc++.h>


vector<int> compareTriplets(vector<int> a, vector<int> b) 
{
    vector<int> points(2,0);
    
    for(int i=0;i<3;i++)
    {
            if(a[i]>b[i])
                points[0]++;
            else if(b[i]>a[i])
                points[1]++;
    }
    return points;
}
